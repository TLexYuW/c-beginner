#include <stdio.h>
#include <string.h>

int main()
{
    char string1[] = "THIS IS String ONE";
    char string2[] = "this s string two";

    // strlwr(string1); // converts a string to lowercase
    // printf("\n%s\n", string1);

    // strupr(string1); // coverts a string to uppercase
    // printf("\n%s\n", string1);

    // strcat(string1, string2); // appends string2 to end of string1
    // printf("\n%s\n",  string1);

    // strncat(string1, string2, 4); // appends n characters from string2 to string1
    // printf("\n%s\n", string1);

    // strcpy(string1, string2); // copy string2 to string1
    // printf("\n%s\n", string1);

    // strncpy(string1, string2, 4); // copy n characters of string2 to string1
    // printf("\n%s\n", string1);

    // strset(string1, '?'); // sets all characters  of a string to a given character
    // printf("\n%s\n", string1);

    // strnset(string1, 'x', 1); // sets first n characters of a string to a given character
    // printf("\n%s\n", string1);

    // strrev(string1); // reverses a string
    // printf("\n%s\n", string1);

    // int result = strlen(string1);               // returs string length as int
    // printf("\n%d", result);
    // int result = strcmp(string1, string2);      // string compare all characters
    // printf("\n%d", result);
    // int result = strncmp(string1, string2, 1);  // string compare n characters
    // printf("\n%d", result);
    // int result = strcmpi(string1, string1);     // string compare all (ignore case)
    // printf("\n%d", result);
    // int result = strnicmp(string1, string1, 1); // string compare n characters (ignore case)
    // printf("\n%d", result);

    return 0;
}