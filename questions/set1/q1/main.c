#include <stdio.h>

int main(int argc, char const *argv[])
{
    // Prinf
    // not only prints the content on the screen,
    // also returns the number of characters
    // that it successfully prints on the screen
    printf("%d", printf("%s", "Hello World!"));

    return 0;
}
